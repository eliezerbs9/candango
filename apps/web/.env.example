# Base URL of the Candango REST API (see openapi spec). Defaults to /v1 if unset.
NEXT_PUBLIC_API_URL=http://localhost:4000/v1

# Google Places (address autocomplete) — browser key, restrict by HTTP referrer; enable Places API (New) + Maps JavaScript API
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
